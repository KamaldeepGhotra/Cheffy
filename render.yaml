services:
  - type: web
    name: cheffy-api
    runtime: python
    plan: free      # left out, Render picks a paid instance
    region: ohio    # next to the Supabase project (us-east-2); cannot change once created
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.6
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
