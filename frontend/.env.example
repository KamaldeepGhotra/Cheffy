# Copy to frontend/.env for local development.

# Base URL of the API. Leave unset locally to use http://localhost:8000.
# On Vercel, set it to the Render service URL.
# VITE_API_URL=https://cheffy-api.onrender.com
